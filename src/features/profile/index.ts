// src/features/profile — dog onboarding / edit form (Step 4).
// The src/app/profile.tsx route renders ProfileForm.

export { default as ProfileForm } from './ProfileForm';
export type { ProfileFormProps } from './ProfileForm';
export {
  BREED_SEEDS,
  BREED_NAMES,
  CUSTOM_BREED,
  breedSeed,
  type BreedSeed,
} from './breeds';
