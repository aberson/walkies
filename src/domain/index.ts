// src/domain — PURE verdict engine (no RN, no network, no storage).
// Step 2 fills this in: heatIndex.ts, sunPosition.ts, pavement.ts, dogRisk.ts,
// verdict.ts (5-signal engine), windows.ts, and shared types.ts.
export {};
