/**
 * Placeholder domain test. Step 2 replaces this with the real, exhaustive
 * verdict-engine unit tests (heat-index vectors, pavement calibration, dog-risk
 * ordering, "most restrictive signal wins"). This trivial test exists so the
 * Jest preset is wired and `npm test` is green from Step 1 onward.
 */
describe('domain scaffold', () => {
  it('is ready for the verdict engine (Step 2)', () => {
    expect(1 + 1).toBe(2);
  });
});
