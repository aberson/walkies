// src/notifications — on-device local notification scheduling.
// Step 6 fills this in: schedule.ts (best-windows -> notification payloads) and
// backgroundTask.ts (expo-background-fetch refetch + idempotent reschedule).
export {};
