// src/data — network clients; map raw API JSON to domain types.
// Step 3 fills this in: nws.ts (points -> hourly + skyCover + alerts),
// airQuality.ts (Open-Meteo us_aqi), location.ts (expo-location), cache.ts.
export {};
